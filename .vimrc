" Starter Vimrc. Some smart defaults to make Vim more usable.

syntax on                   " Turn on syntax highlighting.
filetype plugin indent on   " Load per-filetype indenters.
set showmode                " Mode always displayed at bottom of screen.
set cursorline              " Highlight current line on screen.
set incsearch               " Search as characters are entered.

" Indentation defaults.
set tabstop=4               " Tab width in-editor will be 4 spaces wide.
set shiftwidth=4            " Indentation depth will be 4 spaces.
set expandtab               " Insert spaces instead of <Tab> characters.

